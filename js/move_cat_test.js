describe("pow", function() {
  it("we have a cat!", function() {
  	console.log("cat", cat);
  	assert.notEqual(cat, null);
  });

  it("score is 0", function() {
  	assert.equal(score, 0);
  });
});
